import { Col, Container, Image, Row } from "react-bootstrap";

/** The Home page. */
const Home = () => (
  <main>
    <Container id="landing-page" fluid className="py-3">
      <Row>
        <Col>TextSwap</Col>
      </Row>
    </Container>
  </main>
);

export default Home;
